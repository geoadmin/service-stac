name: PR Auto Labeler

on:
  pull_request:
    types:
      - opened
      - edited
      - synchronized
      - reopened

jobs:
  pr-labeler:
    uses: geoadmin/.github/.github/workflows/pr-labeler.yml@master
